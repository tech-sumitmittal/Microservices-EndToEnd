discovery.docker "sumitbank_ms" {
	host             = "unix:///var/run/docker.sock"
	refresh_interval = "5s"
}

discovery.relabel "sumitbank_ms" {
	targets = []

	rule {
		source_labels = ["__meta_docker_container_name"]
		regex         = "/(.*)"
		target_label  = "container"
	}
}

loki.source.docker "sumitbank_ms" {
	host             = "unix:///var/run/docker.sock"
	targets          = discovery.docker.sumitbank_ms.targets
	forward_to       = [loki.write.default.receiver]
	relabel_rules    = discovery.relabel.sumitbank_ms.rules
	refresh_interval = "5s"
}

loki.write "default" {
	endpoint {
		url       = "http://gateway:3100/loki/api/v1/push"
		tenant_id = "tenant1"
	}
	external_labels = {}
}
